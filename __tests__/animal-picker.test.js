import { hedgehogImageService } from './../src/animal-picker.js';
import { puppyImageService } from './../src/animal-picker.js';

describe('getRandomHedgeHog', () => {
  let animalType = 'hedgie';

  test('should get random image of hedgehog', () => {
    expect()
  })
});

